#!/bin/bash
# PLAIN FUTURE-MI dim-4 H8, div_weight=0 (job 10518578). Run when it finishes.
# Its chain has NO ICC job, so ICC stays ON here.
RUN_DIR=roma_mifuture_dim4_H8_nodiv
TAG=mifut_dim4_nodiv
SKIP_ICC=0
. "$(dirname "$0")/_analyze_common.sh"
