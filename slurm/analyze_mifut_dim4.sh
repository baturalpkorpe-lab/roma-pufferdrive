#!/bin/bash
# FUTURE-MI dim-4 H8, div-on. run_mi_future.sh chains no ICC job on this
# branch, so the ICC runs here.
RUN_DIR=roma_mifuture_dim4_H8
TAG=mifut_dim4
SKIP_ICC=0
. "$(dirname "$0")/_analyze_common.sh"
